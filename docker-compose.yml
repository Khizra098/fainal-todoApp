version: '3.8'

services:
  frontend:
    build:
      context: frontend/
      dockerfile: ../docker/frontend/Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://backend:8000
    env_file:
      - frontend/.env.local
    depends_on:
      - backend
    networks:
      - app-network

  backend:
    build:
      context: backend/
      dockerfile: ../docker/backend/Dockerfile.dev
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - DATABASE_URL=postgresql://todo_user:password@database:5432/todo_app
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    env_file:
      - backend/.env
    depends_on:
      - database
    networks:
      - app-network

  database:
    build:
      context: .
      dockerfile: docker/database/Dockerfile
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init-scripts:/docker-entrypoint-initdb.d
    environment:
      - POSTGRES_DB=todo_app
      - POSTGRES_USER=todo_user
      - POSTGRES_PASSWORD=password
    networks:
      - app-network

volumes:
  postgres_data:

networks:
  app-network:
    driver: bridge